from django.urls import path

from . import views

urlpatterns = [
    # path('logon', views.logon_req, name='logon'),
    # path('record/add', views.add_req, name='add'),
    # path('record/<record_id>/delete', views.delete_req, name='delete'),
    # path('record/<record_id>', views.get_id, name='get'),
]
